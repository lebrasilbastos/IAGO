27. leia o preço de um produto e inflaciona esse preço em 10% se ele for menor que 100 e em 20% se ele for maior ou igual a 100

#include <stdio.h>

int main() {
    float valor, inflacionado;
    printf("Insira o valor: ");
    scanf("%f", &valor);
    
    if (valor >= 100){
        inflacionado = valor * 1.20;
    } else {
        inflacionado = valor * 1.10;
    }
    
    printf("O valor foi: %.2f \nO valor inflacionado agora é: %.2f \n", valor, inflacionado);
    
    return 0;
}
